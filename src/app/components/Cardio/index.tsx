
type CardioProps = {
    updateFunction:any
}

const Cardio = ({updateFunction}:CardioProps) => {
    return (
        <div>
          <h2>Cardio</h2>
          <button>BUTTON</button>
        </div>
    )
};

export default Cardio
