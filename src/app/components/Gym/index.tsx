type GymProps = {
    updateFunction:any 
    //must fix later
}

const Gym = ({updateFunction}:GymProps) => {
    return (
        <div className="gym-card">
            <h2>Gain muscle</h2>
            <button>BUTTON</button>
          
        </div>
    )
};

export default Gym
